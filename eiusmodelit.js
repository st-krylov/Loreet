const enter = {
  x: 10,
  y: 20,
  z: 30,
};

const result = processCoordinates({
  x: enter.x,
  y: enter.y, // Assigning enter.y to the y property
});

console.log(result); // The result of processing the coordinates
